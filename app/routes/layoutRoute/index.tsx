export default function Index() {
  return (
    <div style={{ fontFamily: "system-ui, sans-serif", lineHeight: "1.4" }}>
     Index.tsx deleted for conflicting with routes/index.tsx
     - keep an eye on pathless routes and conflicting index.tsx files
    </div>
  );
}
